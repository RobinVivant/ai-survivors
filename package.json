{
  "name": "ai-survivors",
  "private": true,
  "scripts": {
    "dev": "DEV=1 bun run server/server.ts",
    "server": "bun run scripts/generate-assets.ts && bun run server/server.ts",
    "build:web": "bash scripts/build.sh",
    "build:desktop": "bash scripts/build-desktop.sh",
    "desktop": "bun run server/desktop.ts"
  },
  "devDependencies": {
    "webview-bun": "^2.4.0"
  }
}
